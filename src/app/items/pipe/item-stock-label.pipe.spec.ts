import { ItemStockLabelPipe } from './item-stock-label.pipe';

describe('ItemStockLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemStockLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
