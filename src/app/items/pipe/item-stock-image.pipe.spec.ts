import { ItemStockImagePipe } from './item-stock-image.pipe';

describe('ItemStockImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemStockImagePipe();
    expect(pipe).toBeTruthy();
  });
});
